!TorchvisionEncoder
parameters:
  model_dir: /
  model_name: vgg16
  _use_cuda: true
  layers:
    - features
    - avgpool
    - x.view(x.size(0), -1)
    - classifier[0]
gnes_config:
  name: vgg_encoder
  is_trained: true
  work_dir: /save

